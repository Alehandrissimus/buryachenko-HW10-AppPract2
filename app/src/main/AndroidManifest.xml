<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buryachenko_hw10_apppract2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuryachenkoHW10AppPract2">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstTaskActivity"/>
        <activity android:name=".SecondTaskActivity"/>
        <activity android:name=".SecondTaskAdditionalActivity"/>
        <activity android:name=".ThirdTaskActivity"/>

        <receiver android:name=".NotificationBroadcast">
            <intent-filter>
                <action android:name="NOTIFICATION_REPLY"/>
            </intent-filter>
        </receiver>


    </application>

</manifest>